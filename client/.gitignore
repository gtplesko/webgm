# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production

# This is commented out because we want to check build into VC, since we don't
# have any "build on deploy" flow set up yet. If we set that up, we should
# remove /build from VC.
# /build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
